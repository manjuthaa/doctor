#include <stdio.h>
int main()
{
char string[200],ch;
int count=0,i=0;

gets(string);
while(string[i]!='\0')
{
	if(string[i]==' ')
	count++;
	i++;
}
printf("number of blank spaces in the given string %d",count);
return 0;
}

